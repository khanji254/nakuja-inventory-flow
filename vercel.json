{
  "builds": [
    { "src": "api/**/*.ts", "use": "@vercel/node@3" },
    { "src": "package.json", "use": "@vercel/static-build@2", "config": { "distDir": "dist" } }
  ],
  "routes": [
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
